import { PokedexNamePipe } from './pokedex-name.pipe';

describe('PokedexNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PokedexNamePipe();
    expect(pipe).toBeTruthy();
  });
});
