import { PokedexDescriptionPipe } from './pokedex-description.pipe';

describe('PokedexDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new PokedexDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
